name: 'ACTIONGO'

on: [push]

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v2  
      with:
          sfdx-auth-url: "00D80000000M5JG!AQoAQK5AHUisfZG0hM.7Vhaf2Uczbj2KAuYBnKv7XrEHif8wlTAS0wghWSS9_CGTzRKbtJw6G_._fjbLKOobF3lfSUkG.4ra"  
    - name: Install Salesforce CLI
      run: |
          wget https://developer.salesforce.com/media/salesforce-cli/sfdx-linux-amd64.tar.xz
          mkdir sfdx-cli
          tar xJf sfdx-linux-amd64.tar.xz -C sfdx-cli --strip-components 1
          ./sfdx-cli/install
    - name: version
      run: sfdx version
    - name: Project
      run: sfdx force:project:create -n "Action"
    
    - name: creation
      run: sfdx force:org:create -f config/project-scratch-def.json -d 3 -v santosh.n@servicemax.com -a SVMX-GITHUBACTION -d 30 
      
      
        
 
